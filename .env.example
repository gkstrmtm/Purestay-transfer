# Local dev server
PORT=5173

# Optional: protect /api/submissions
# ADMIN_TOKEN=change-me
# Backend admin token (used by /backend page)
# Generate a strong random value and keep it secret.
ADMIN_TOKEN=

# Cron endpoints security:
# - On Vercel Cron, requests include `x-vercel-cron` and will run.
# - For manual triggering (local/dev), set ADMIN_TOKEN and call with `?token=...`.

# Storage (choose one)
#
# Option A (recommended for you): Supabase Postgres
# These come from your Supabase project Settings -> API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Note: server-side storage + portal require SUPABASE_SERVICE_ROLE_KEY.
# (Using only an anon key is intentionally unsupported.)

#
# Option B: Vercel KV (optional fallback)
# If you create a KV store in Vercel, these are typically injected automatically.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# --- AI blog generation ---
# Required for blog automation
AI_API_KEY=

# Optional overrides (defaults are fine)
# AI_API_BASE_URL=https://api.openai.com/v1
# AI_MODEL=gpt-4o-mini
# AI_TIMEOUT_MS=45000

# Optional: used for canonical URLs in SEO output
# SITE_URL=https://purestaync.com

# --- Portal ---
# Optional override for demo-user password used by POST /api/portal/seed
# PORTAL_DEMO_PASSWORD=PurestayDemo!234

# --- Portal notifications (email/SMS are env-var gated) ---
# Email (Resend-compatible)
RESEND_API_KEY=
NOTIFY_FROM_EMAIL=PureStay <no-reply@purestay.com>

# SMS (Twilio-compatible)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_PHONE=

# Report sending recipients
# If an event can be mapped to an account owner, the report sends to that owner.
# Otherwise it falls back to REPORT_TO_ROLES + REPORT_TO_EMAILS.
REPORT_TO_ROLES=account_manager,manager
REPORT_TO_EMAILS=

# Optional auto-CC for reports (UI-supplied CC overrides this)
REPORT_CC_ROLES=manager
REPORT_CC_EMAILS=

# Reminder cron CC defaults
# Event coordinators are CC'd per-event from assignments. These envs define oversight CC.
REMINDER_CC_ROLES=manager
REMINDER_CC_EMAILS=


