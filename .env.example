# Local dev server
PORT=5173

# Optional: protect /api/submissions
# ADMIN_TOKEN=change-me
# Backend admin token (used by /backend page)
# Generate a strong random value and keep it secret.
ADMIN_TOKEN=

# Vercel KV (recommended for production persistence)
# If you create a KV store in Vercel, these are typically injected automatically.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# --- AI blog generation ---
# Required for blog automation
AI_API_KEY=

# Optional overrides (defaults are fine)
# AI_API_BASE_URL=https://api.openai.com/v1
# AI_MODEL=gpt-4o-mini
# AI_TIMEOUT_MS=45000

# Optional: used for canonical URLs in SEO output
# SITE_URL=https://purestaync.com

# Optional: minimum days between automated posts (cron can run daily; generator will skip if too soon)
# BLOG_AUTOGEN_MIN_DAYS=2
